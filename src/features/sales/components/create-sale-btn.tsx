import { Button } from "@/components/ui/button";
import { Calculator } from "lucide-react";

const CreateSaleBtn = () => {
  return (
    <Button>
      <Calculator /> Crear venta
    </Button>
  );
};

export default CreateSaleBtn;
